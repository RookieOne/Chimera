<UserControl x:Class="Example.Shell.DisplayView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:MDI="clr-namespace:WPF.MDI;assembly=WPF.MDI" mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>
        
        <Menu x:Name="menu" />

        <MDI:MdiContainer x:Name="mdiContainer" 
                          Grid.Row="2"
                          Background="#00000000"
                          Panel.ZIndex="-10"
                          />
    </Grid>
</UserControl>
